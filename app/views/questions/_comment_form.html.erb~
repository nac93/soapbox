<%= form_for(@comment1 = @choice1.comments.build(:choice_id => @choice1.id)) do |f| %>

  <%= render 'comment_fields', f: f %>


  <%= f.submit "Post", :choice_id => @choice1.id, class: "btn btn-large btn-primary" %>
<% end %>
